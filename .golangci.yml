run:
  timeout: 5m
  go: "1.21.6"

linters-settings:
  gofmt:
    simplify: true
  govet:
    enable-all: true
  goconst:
    min-len: 2
    min-occurrences: 2
  goimports:
    local-prefixes: github.com/yan-aint-nickname/tanu
  misspell:
    locale: US
  lll:
    line-length: 120


linters:
  disable-all: true
  enable:
    - lll
    - gofmt
    - revive
    - gosimple
    - govet
    - ineffassign
    - unconvert
    - unused
    - errcheck
    - dogsled
    - dupl
    - gocritic
    - gocyclo
    - goimports
    - gosec
    - misspell
    - nakedret
    - staticcheck
    - stylecheck
    - typecheck
    - unparam
    - whitespace
